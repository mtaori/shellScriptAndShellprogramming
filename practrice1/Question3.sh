#!/bin/bash
HOSTNAME="bash"
MY_SHELL="Shell Scripting"
if [ $HOSTNAME="bash" && $MY_SHELL="Shell Scripting" ]
then
	echo "This script is running on ${HOSTNAME}. where ${MY_SHELL} is fun."
else
	echo "Check you program again	"
fi
